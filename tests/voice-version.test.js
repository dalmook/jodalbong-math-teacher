import test from 'node:test';import assert from 'node:assert/strict';import {createVoiceServer} from '../server.mjs';
test('official realtime v3 uses v1 catalog, not the incompatible v2 voices',async t=>{
 const app=createVoiceServer({port:0,clientFactory:()=>({async open(){},async close(){return true},async rpc(m){if(m==='account/read')return {account:{type:'chatgpt'}};if(m==='thread/realtime/listVoices')return {voices:{v1:['juniper','cove'],v2:['marin','cedar'],defaultV1:'cove',defaultV2:'marin'}};throw Error('unexpected RPC')}})});await app.listen();t.after(()=>app.close());const page=await fetch(app.origin);const cookie=page.headers.get('set-cookie').split(';')[0];const r=await fetch(app.origin+'/api/catalog',{method:'POST',headers:{cookie,origin:app.origin,'X-Voice-Request':'1','Content-Type':'application/json'},body:'{}'});assert.equal(r.status,200);const result=await r.json();assert.deepEqual(result.voices,['juniper','cove']);assert.equal(result.defaultVoice,'juniper');
});
